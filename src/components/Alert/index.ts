import AlertRoot from "./AlertRoot.astro";
import AlertTitle from "./AlertTitle.astro";
import AlertDescription from "./AlertDescription.astro";

const Alert = {
    Root: AlertRoot,
    Title: AlertTitle,
    Description: AlertDescription,
}

export default Alert