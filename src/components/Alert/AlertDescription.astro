---
interface Props {
    class?: string;
    id?: string;
}
const {
    class: className,
    id: alertId,
} = Astro.props
---

<div class={`pl-1 ${className}`} id={`${alertId}`}>
    <slot />
</div>